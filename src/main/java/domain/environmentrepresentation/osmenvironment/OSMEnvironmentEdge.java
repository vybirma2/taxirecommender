package domain.environmentrepresentation.osmenvironment;

import domain.environmentrepresentation.EnvironmentEdge;

public class OSMEnvironmentEdge extends EnvironmentEdge {

    public OSMEnvironmentEdge(int fromId, int toId, float speed, int length, int time) {
        super(fromId, toId, speed, length, time);
    }
}
